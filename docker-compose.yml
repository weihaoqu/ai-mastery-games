services:
  app:
    build: .
    ports:
      - "3100:3000"
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
    volumes:
      - app-data:/app/.data
    restart: unless-stopped

volumes:
  app-data:
